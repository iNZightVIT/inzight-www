{
"index" : {
  "title" : "Variables",
  "desc"  : "iNZight assumes that the data set you import is structured into rows = cases, and columns = variables. These variables are either numeric or categorical, each of which has its own set of operations."
}
}
